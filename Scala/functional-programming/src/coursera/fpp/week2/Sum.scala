package coursera.fpp.week2

/**
 * Created by christian.sisti on 07/12/2013.
 */
object Sum {
  def sum(f: Int => Int)(a: Int, b: Int): Int = {
    def loop(a: Int, acc: Int): Int = {
      if (???) ???
      else loop(???, ???)
    }
    loop(???, ???)
  }
}